- Verify `specs/$ARG.md` against implementation
- $ARG == "all" → verify all specs; never assume "all", unless explicit

Report: ✅ correct | ⚠️ differs | ❌ missing

Ask user: (1) verify only (2) implement (3) sync spec
