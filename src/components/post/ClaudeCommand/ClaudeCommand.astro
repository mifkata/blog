---
import GitHub from "@/components/ui/icons/GitHub.astro";
import Globe from "@/components/ui/icons/Globe.astro";

interface Props {
  name: string;
  external?: boolean;
}

const { name, external = false } = Astro.props;

const url = external
  ? "https://code.claude.com/docs/en/slash-commands"
  : `https://github.com/mifkata/blog/blob/main/.claude/commands/${name}.md`;

const title = external
  ? `View the official documentation for the /${name} command on Claude's website`
  : `View the source for the /${name} command on GitHub`;
---

<a
  href={url}
  target="_blank"
  rel="noopener noreferrer"
  title={title}
  class="inline-flex items-center gap-1 font-mono text-sm text-accent hover:text-accent-dark no-underline relative top-[3px]"
>
  {external ? <Globe size={16} /> : <GitHub size={16} />}
  /{name}
  <span class="sr-only">(opens in new tab)</span>
</a>
