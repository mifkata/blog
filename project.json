{
  "name": "blog",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["astro dev", "storybook dev -p 6006 --no-open"],
        "parallel": true
      }
    }
  }
}
