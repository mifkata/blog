{
  "name": "blog",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["pnpm dev:astro", "pnpm storybook"],
        "parallel": true
      }
    }
  }
}
