# CurrentlyReading

`src/components/CurrentlyReading/CurrentlyReading.astro` - Reading list section.

Props: none

Data: imports from `@/data/books.json` (array of {title, author, url})

Output: section with heading + styled list of book links

Visual: `bg-gray-light/50 p-6 rounded-lg border-l-4 border-accent`

Book: linked title (accent) + author
