# ContactCTA

`src/components/pages/common/ContactCTA/ContactCTA.astro` - Contact call-to-action section.
Props: `headline?` (default: "Want to work together?"), `description?` (default: "I'm available for contract engagements..."), `buttonText?` (default: "Get in Touch"), `buttonHref?` (default: "mailto:hi@mifkata.com")
Output: `<section class="text-center py-12 border-t border-gray-light">` → headline + description + slot + Button (inverse)
Uses: Button
