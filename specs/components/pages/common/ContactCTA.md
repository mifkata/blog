# ContactCTA

`src/components/pages/common/ContactCTA/ContactCTA.astro` - Contact call-to-action section.

## Props

- `headline?: string` (default: "Want to work together?")
- `description?: string` (default: "I'm available for contract engagements...")
- `buttonText?: string` (default: "Get in Touch")
- `buttonHref?: string` (default: "mailto:hi@mifkata.com")

## Output

- `<section class="text-center py-12 border-t border-gray-light">`
- Headline + description + slot + Button (inverse)

## Dependencies

- Button
