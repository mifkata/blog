1. Run `git diff --staged --stat` and `git diff --staged`
2. Determine type: feat|fix|refactor|docs|test|chore|style|perf|build|ci
3. Warn if no staged changes or if changes should be split
4. Commit with message
